---
import styles from './ActionGroup.module.css';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={[styles.group, className]}>
  <slot />
</div>
