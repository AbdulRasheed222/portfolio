---
import type { PublicationEntry } from '../../lib/publications';
import styles from './PublicationStatusNote.module.css';

interface Props {
  note: string;
}

const { note } = Astro.props;
---

<aside class={styles.note} role="note">
  <p>{note}</p>
</aside>
